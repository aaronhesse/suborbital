add a target box a-la gmail file drop
change ip address to anonymous user animal icon (a-la gmail)
figure out a way to make the bar hidden by default, and then show it when file is dropped?
add % text to the progress bar (maybe later)
change chrome warning to be a top banner like "save this password" box in chrome?